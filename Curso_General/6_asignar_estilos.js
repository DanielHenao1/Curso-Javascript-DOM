const primerTopping = document.querySelector(".topping");
// console.log(primerTopping); Para mostrar si tenemos acceso a ese elemento no lo arroja por consola

// console.log(primerTopping.style); // Aca podemos ver que estilos tiene asignados un elemento o cuales le podemos asignar

// Vamos a asignarle estilos a la variable primerTopping
primerTopping.style.backgroundColor = "blue";
primerTopping.style.color = "#6dff00";
primerTopping.style.fontSize = "2rem";
primerTopping.style.textTransform = "uppercase";
