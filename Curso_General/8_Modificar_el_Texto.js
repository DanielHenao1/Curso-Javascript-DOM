const titulo = document.getElementById('titulo');
console.log(titulo); // para verificar se está acessando o elemento corretamente

// Asi editamos o texto dentro do elemento 
titulo.innerText = 'Mis Toppings Favoritos'